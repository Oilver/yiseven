import { RecordTypePipe } from './record-type.pipe';

describe('RecordTypePipe', () => {
  it('create an instance', () => {
    const pipe = new RecordTypePipe();
    expect(pipe).toBeTruthy();
  });
});
